# 実動作確認

release-verified/results.json：ローカル公開用出力で14ケース/28セクションを確認、失敗0。Chromium通常6幅（1440/1024/768/390/320/短い390）、低モーションPC/SP、写真通信失敗PC/SP、JS無効PC/SP、WebKit通常PC/SP。各リンク44px以上、文章各行の左/中心/右と個別矢印のヒット判定、横あふれ/文字の外枠越えなし、実写真の回転/アニメーションなし。TabおよびmacOS WebKitのOption+Tabで表示されるフォーカスと順序を確認。

通常PC/SPで4声をクリックしvoice.html#v01/v02/v08/v33の全文を実際に開き、地域/様邸と原文を同じデータIDと照合。v01/v08の同姓表記を混同しない。全件導線からvoice.htmlの実際の.vo一覧を確認。最初のQA版は別ページに存在しない見出しを待っていたため、実装を変更せず検証対象を既存の.vo一覧へ修正して再実行した。

予約/資料請求の両ボタンは既存contactDialogの対応する案内見出しを開く。Escape/閉じるで閉鎖し起点へフォーカス復帰。電話hrefはtel:0742361123。実電話発信や問い合わせ送信は行わない。既存ダイアログは送信フォームではなく制作確認用の案内で、今回その接続状態は変えていない。JS無効では写真・本文・通常リンク/電話リンクを保持するが、既存の案内ダイアログと全文モーダルはJSが必要。

写真エラー時は写真を隠し読込失敗表示、本文とリンクを維持。低モーションとJS無効で装飾を静止表示。外部API/個人情報入力は変更していない。29件の既存データ/計算/API回帰テスト、ホームページ11・FV5・Nara4の故障検出例も通過。

右上花のWebP解像度/圧縮とSPリビングの切り取り/必要画像幅を最終調整し、公開ビルドを作り直した。その最終出力で14ケースを再実行して失敗0、採用画像との比較画像と証跡を更新した。公開後には本番の通常8ケースを同じ操作で再確認する。
