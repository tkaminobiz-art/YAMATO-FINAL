# 動作確認

最終版：66件のNodeテスト成功（既存55＋学校検索11）、294項目のブラウザー確認成功、38画面、JS例外0、横あふれ0。
Chromium1440×900/1280×720/1024×768、WebKit1280×800/390×844/320×740。実機ではない。詳細はlocal-browser.json、unit-results.txt。

PCの一覧URLでも地図が表示される。全40件の元座標が初期表示範囲に入ることを、実際に返されたLeafletインスタンスへの読取用参照で確認。コード挿入はこの参照を保持するテスト観測のみで、地図の動作・ソースデータは置換していない。ズーム、ドラッグ、一覧移動、地図選択、全体表示、スクロールは画面の操作を使った。
ページのホイールスクロールで地図が画面外へ流れ、ホイールで地図はズームしない。地図・一覧に個別スクロール枠はない。拡大表示はボタン操作のみで開閉、Escapeで復帰。PC/SP切替後も一覧・地図の状態を保持。
7つの並び順をデータと照合。学校の同値はIDで安定、欠損は末尾。学校2条件のAND、URL・再読込・条件の一部解除・0件・リセットを確認。リセットは以前の地域選択も解除。地図範囲付きURLの詳細→戻るで範囲・一覧を保持。URLのみを同じタブで変更した場合も検索結果を更新。スクロール復元はアプリ側に一本化。
保存・比較2件・掲載距離の比較表示・試算への土地価格引継ぎと結果表示をChromiumPCとWebKitSPで確認。返済額の計算関数getEstimateは旧snapshotと本文が完全一致。物件原典・画像・金融設定・相談API本体も保持。
タイルを全件失敗させた別のChromium環境で、エラー表示から40件一覧へ移れることを実クリックで確認。実際の問い合わせ・外部送信はしていない。

配布snapshot・実行ソース・配布パッケージの24ファイルはSHA一致。配布側のAPIは追加モジュールを含めimport成功。公開先での動作・rewrite・HTTP API応答は未確認で、ディレクターによる公開後の確認が必要。
