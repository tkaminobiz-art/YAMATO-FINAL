--- before/land-payment-study.html
+++ after/land-payment-study.html
@@ -10,15 +10,15 @@
 <main id="main">
 <section id="search-screen" aria-labelledby="search-heading">
  <div class="search-heading"><div><p class="eyebrow">奈良・京都の土地探し</p><h1 id="search-heading" tabindex="-1">土地を探す</h1></div><p class="desktop-intro">気になる土地を保存して、<br>周辺環境や返済額を比べられます。</p></div>
- <div class="search-toolbar"><button id="open-filters" class="filter-toggle">絞り込み <b id="filter-count"></b></button><div class="view-switch" role="group" aria-label="表示を切り替える"><button id="map-view" aria-pressed="true">地図</button><button id="list-view" aria-pressed="false">一覧</button></div><p id="count" role="status">読み込み中</p><label class="sort-label">並び順<select id="sort"><option value="price">価格が安い順（下限）</option><option value="area">面積が広い順（上限）</option><option value="walk">駅から近い順（徒歩）</option><option value="updated">情報が新しい順</option><option value="region">地域順</option></select></label></div>
+ <div class="search-toolbar"><button id="open-filters" class="filter-toggle">絞り込み <b id="filter-count"></b></button><div class="view-switch" role="group" aria-label="表示を切り替える"><button id="map-view" aria-pressed="true">地図</button><button id="list-view" aria-pressed="false">一覧</button></div><p id="count" role="status">読み込み中</p><a class="results-jump" href="#property-results" data-scroll-to="property-results">一覧を見る ↓</a><label class="sort-label">並び順<select id="sort"><option value="price">価格が安い順（下限）</option><option value="area">面積が広い順（上限）</option><option value="walk">駅から近い順（徒歩）</option><option value="primary">小学校が近い順</option><option value="junior">中学校が近い順</option><option value="updated">情報が新しい順</option><option value="region">地域順</option></select></label></div>
  <div id="active-filters" class="active-filters"></div>
- <div class="workspace" id="workspace"><aside class="list-panel" aria-label="検索結果"><p id="match-note" class="list-note"></p><div id="list"></div></aside><div class="map-column"><div id="map-shell" class="map-shell"><div id="property-map" role="region" aria-label="奈良県・京都府の土地の地図"></div><div class="map-caption"><span>位置は目安</span><span>数字は掲載件数</span></div><button id="map-expand" class="map-expand" aria-label="地図を大きく見る">拡大表示</button><button id="map-close" class="map-close" hidden>地図を閉じる</button><button id="search-bounds" class="bounds-button" hidden>この範囲で探す</button><div id="map-error" class="map-error" hidden><p>地図を読み込めませんでした。</p><button id="fallback-list">一覧から探す</button></div><div id="map-empty" class="map-empty" role="status" hidden><h2>条件に合う物件が見つかりません</h2><p>地域や予算を変えて、もう一度お探しください。</p><button data-reset-filters>絞り込みを解除</button></div></div><div id="selected-card" class="selected-card" hidden></div><p class="map-footnote">地図の位置はSUUMOの掲載情報をもとにした目安です。正確な区画や入口は担当者にご確認ください。</p></div></div>
+ <div class="workspace" id="workspace"><div class="map-column"><div id="map-shell" class="map-shell"><div id="property-map" role="region" aria-label="奈良県・京都府の土地の地図"></div><div class="map-caption"><span>位置は目安</span><span>数字は掲載件数</span></div><button id="map-fit" class="map-fit" aria-label="検索結果の全体を地図に表示">全体を見る</button><button id="map-expand" class="map-expand" aria-label="地図を大きく見る">拡大表示</button><button id="map-close" class="map-close" hidden>地図を閉じる</button><button id="search-bounds" class="bounds-button" hidden>この範囲で探す</button><div id="map-error" class="map-error" hidden><p>地図を読み込めませんでした。</p><button id="fallback-list">一覧から探す</button></div><div id="map-empty" class="map-empty" role="status" hidden><h2>条件に合う物件が見つかりません</h2><p>地域や予算を変えて、もう一度お探しください。</p><button data-reset-filters>絞り込みを解除</button></div></div><div id="selected-card" class="selected-card" hidden></div><p class="map-footnote">地図の位置はSUUMOの掲載情報をもとにした目安です。正確な区画や入口は担当者にご確認ください。</p></div><aside id="property-results" class="list-panel" aria-label="検索結果"><div class="results-heading"><h2>物件一覧</h2><div class="results-tools"></div></div><p id="school-note" class="list-note" hidden></p><p id="match-note" class="list-note"></p><div id="list"></div></aside></div>
  <p class="catalog-note">掲載40件には複数の区画を含む物件があります。情報提供日：2026年9月6日／取得日：9月7日</p>
 </section>
 <div id="content-screen" hidden></div>
 </main>
 <aside id="compare-tray" class="compare-tray" aria-label="比較する土地" hidden></aside><div id="toast" class="toast" role="status" aria-live="polite" hidden></div>
-<dialog id="filter-dialog" aria-labelledby="filter-heading"><form id="filter-form"><div class="dialog-head"><h2 id="filter-heading">絞り込み</h2><button type="button" data-close-dialog aria-label="絞り込みを閉じる">×</button></div><fieldset><legend>地域</legend><div id="region-options"></div></fieldset><label>土地価格の上限<select name="price"><option value="">指定なし</option><option value="1000">1,000万円まで</option><option value="1500">1,500万円まで</option><option value="2000">2,000万円まで</option><option value="2500">2,500万円まで</option><option value="3000">3,000万円まで</option></select></label><label>土地面積<select name="area"><option value="">指定なし</option><option value="150">150㎡以上（約45坪）</option><option value="180">180㎡以上（約54坪）</option></select></label><label>駅までの徒歩時間<select name="walk"><option value="">指定なし</option><option value="10">10分以内</option><option value="15">15分以内</option><option value="20">20分以内</option></select></label><p class="note">価格や面積に幅がある物件は、ご希望の両方を満たす区画があるか担当者にご確認ください。徒歩時間は鉄道駅までの時間です。</p><div class="dialog-actions"><button type="reset">リセット</button><button type="submit" class="primary">検索する</button></div></form></dialog>
+<dialog id="filter-dialog" aria-labelledby="filter-heading"><form id="filter-form"><div class="dialog-head"><h2 id="filter-heading">絞り込み</h2><button type="button" data-close-dialog aria-label="絞り込みを閉じる">×</button></div><fieldset><legend>地域</legend><div id="region-options"></div></fieldset><label>土地価格の上限<select name="price"><option value="">指定なし</option><option value="1000">1,000万円まで</option><option value="1500">1,500万円まで</option><option value="2000">2,000万円まで</option><option value="2500">2,500万円まで</option><option value="3000">3,000万円まで</option></select></label><label>土地面積<select name="area"><option value="">指定なし</option><option value="150">150㎡以上（約45坪）</option><option value="180">180㎡以上（約54坪）</option></select></label><label>駅までの徒歩時間<select name="walk"><option value="">指定なし</option><option value="10">10分以内</option><option value="15">15分以内</option><option value="20">20分以内</option></select></label><p class="note">価格や面積に幅がある物件は、ご希望の両方を満たす区画があるか担当者にご確認ください。徒歩時間は鉄道駅までの時間です。</p><details id="school-filters" class="school-filters"><summary>学校までの距離</summary><p class="note">SUUMOに掲載された学校までの距離です。通う学校・通学路は未確認です。</p><div class="school-filter-fields"><label>小学校<select name="primary"><option value="">指定なし</option><option value="800">800m以内</option><option value="1200">1,200m以内</option><option value="1600">1,600m以内</option></select></label><label>中学校<select name="junior"><option value="">指定なし</option><option value="800">800m以内</option><option value="1200">1,200m以内</option><option value="1600">1,600m以内</option></select></label></div></details><div class="dialog-actions"><button type="reset">リセット</button><button type="submit" class="primary">検索する</button></div></form></dialog>
 <dialog id="cluster-dialog" aria-labelledby="cluster-heading"><div class="dialog-head"><h2 id="cluster-heading">この付近の物件</h2><button data-close-dialog aria-label="物件一覧を閉じる">×</button></div><div id="cluster-items"></div></dialog>
 <dialog id="image-dialog" class="image-dialog" aria-label="物件の写真を拡大"><button data-close-dialog class="image-close">閉じる ×</button><div id="image-content"></div></dialog>
 <noscript><p>土地検索や返済額の計算には、JavaScriptを有効にしてください。<a href="https://suumo.jp/tochi/__JJ_JJ010FJ001_arz1060z2bsz1030z2kkcz1140582001.html">SUUMOの掲載一覧へ</a></p></noscript>
--- before/assets/land-payment-study/app.mjs
+++ after/assets/land-payment-study/app.mjs
@@ -22,10 +22,10 @@
  const ids=store.get().compareIds,show=ids.length&& !['compare','inquiry','estimate','result'].includes(store.get().route.page);$('#compare-tray').hidden=!show;document.body.classList.toggle('has-tray',!!show);$('#compare-tray').innerHTML=show?`<div><strong>${ids.length}件を選択中</strong><small>${ids.length<2?'あと1件選んでください':'建物・返済条件をそろえて比較'}</small></div><button class="primary" data-page="compare" ${ids.length<2?'disabled':''}>比較する ${icon('arrow')}</button>`:'';
 }
 function searchUpdate(options={}){const s=store.get().search;if(s.selectedId&&!queryCatalog(s.filters,s.sort).some(r=>r.property.id===s.selectedId))store.update(st=>{st.search.selectedId=null;});const items=renderSearch(store,options);map.update(items,store.get().search.selectedId,store.get().search,options);$('#search-bounds').hidden=!store.get().search.boundsPending;}
-function select(id,pan=false){store.update(s=>{s.search.selectedId=id;s.search.view='map';});renderSearch(store,{rebuild:false});map.update(queryCatalog(store.get().search.filters,store.get().search.sort).map(r=>r.property),id,store.get().search);map.select(id,{pan});if(map.isFull())map.fullscreen(false);if(pan&&innerWidth<=900){$('#map-shell').scrollIntoView({block:'start'});$('#selected-card a')?.focus({preventScroll:true});}router.replace();}
-function render(route,{restored=false}={}){
+function select(id,pan=false){store.update(s=>{s.search.selectedId=id;s.search.view='map';});renderSearch(store,{rebuild:false});map.update(queryCatalog(store.get().search.filters,store.get().search.sort).map(r=>r.property),id,store.get().search);map.select(id,{pan});if(map.isFull())map.fullscreen(false);if(pan){$('#map-shell').scrollIntoView({block:'start'});$('#selected-card a')?.focus({preventScroll:true});}else if(innerWidth>900){$('#selected-card').scrollIntoView({block:'nearest'});}router.replace();}
+function render(route,{restored=false,fitBounds=false}={}){
  map.fullscreen(false);$('#search-screen').hidden=route.page!=='search';$('#content-screen').hidden=route.page==='search';
- if(route.page==='search'){searchUpdate({restore:restored});requestAnimationFrame(()=>{scrollTo(0,store.get().search.scroll);$('.list-panel').scrollTop=store.get().search.listScroll;});}
+ if(route.page==='search'){searchUpdate({restore:restored&&!fitBounds,fitBounds});requestAnimationFrame(()=>{scrollTo(0,store.get().search.scroll);$('.list-panel').scrollTop=store.get().search.listScroll;});}
  else {const views={property:()=>propertyView(store,route.id),saved:()=>savedView(store),estimate:()=>estimateView(store,route.id),result:()=>resultView(store,route.id),compare:()=>compareView(store),inquiry:()=>inquiryView(store)};$('#content-screen').innerHTML=(views[route.page]??views.saved)();scrollTo(0,0);}
  updateNav();if(!restored)requestAnimationFrame(()=>$(route.page==='search'?'#search-heading':'#content-screen h1')?.focus({preventScroll:true}));
 }
@@ -65,7 +65,7 @@
  if(b.dataset.acceptFinance){store.update(s=>{const d=s.drafts[s.route.id];d.common.financeId=b.dataset.acceptFinance;d.common.years=Number(b.dataset.years);s.common=structuredClone(d.common);});refreshContent();router.replace();return;}
  if(b.id==='download-inquiry'||b.id==='copy-inquiry'){try{const body=buildInquiry(store.get());validateInquiry(body);const text=inquiryText(body);if(b.id==='download-inquiry'){const url=URL.createObjectURL(new Blob([text],{type:'text/plain;charset=utf-8'})),a=document.createElement('a');a.href=url;a.download='やまと不動産_相談内容_未送信.txt';a.click();setTimeout(()=>URL.revokeObjectURL(url),1000);toast('相談内容を保存しました。まだ送信されていません。');}else navigator.clipboard.writeText(text).then(()=>toast('相談内容をコピーしました。まだ送信されていません。')).catch(()=>toast('コピーできませんでした。「相談内容を保存」をお使いください。'));}catch(error){$('#inquiry-error').hidden=false;$('#inquiry-error').textContent=error.message;}return;}
  if(b.id==='close-card'){store.update(s=>{s.search.selectedId=null;});renderSelection(store);map.select(null);router.replace();return;}
- if(b.dataset.removeFilter){store.update(s=>{if(b.dataset.removeFilter==='regions')s.search.filters.regions=[];else delete s.search.filters[b.dataset.removeFilter];});searchUpdate();router.replace();return;}
+ if(b.dataset.removeFilter){store.update(s=>{if(b.dataset.removeFilter==='regions')s.search.filters.regions=[];else delete s.search.filters[b.dataset.removeFilter];});searchUpdate({fitBounds:!store.get().search.filters.bounds});router.replace();return;}
  if(b.matches('[data-reset-filters]')){store.update(s=>{s.search.filters={regions:[]};s.search.boundsPending=false;});searchUpdate({fitBounds:true});router.replace();return;}
 });
 document.addEventListener('input',e=>{if(e.target.closest('#estimate-form'))saveEstimateForm(e.target.closest('form'));if(e.target.dataset.budget){store.update(s=>{s.budgets[e.target.dataset.budget]=e.target.value;});refreshComparisonFigures();e.target.closest('label').nextElementSibling.textContent='入力した予算で計算';}});
@@ -76,11 +76,15 @@
  if(form.id==='destination-form'){e.preventDefault();store.update(s=>{s.common=readCommon(form,s.common);});router.replace();toast('通勤先を保存しました。比較・相談画面で確認できます。');}
 });
 $('#map-view').onclick=()=>{store.update(s=>{s.search.view='map';});searchUpdate({rebuild:false});router.replace();};
-$('#list-view').onclick=$('#fallback-list').onclick=()=>{store.update(s=>{s.search.view='list';});renderSearch(store,{rebuild:false});router.replace();};
+$('#list-view').onclick=()=>{store.update(s=>{s.search.view='list';});renderSearch(store,{rebuild:false});router.replace();};
+$('#fallback-list').onclick=()=>{store.update(s=>{s.search.view='list';});renderSearch(store,{rebuild:false});router.replace();if(innerWidth>900)$('#property-results').scrollIntoView({block:'start'});};
 $('#sort').onchange=e=>{store.update(s=>{s.search.sort=e.target.value;});searchUpdate();router.replace();};
 $('#open-filters').onclick=()=>{fillFilters(store);$('#filter-dialog').showModal();};
-$('#filter-form').onsubmit=e=>{e.preventDefault();const data=new FormData(e.target),previous=store.get().search.filters.regions.join();store.update(s=>{s.search.filters=sanitizeFilters({regions:data.getAll('region'),price:data.get('price'),area:data.get('area'),walk:data.get('walk'),bounds:s.search.filters.bounds});});$('#filter-dialog').close();searchUpdate({fitBounds:previous!==store.get().search.filters.regions.join()});router.replace();};
+$('#filter-form').onreset=e=>{e.preventDefault();e.target.querySelectorAll('[name=region]').forEach(n=>n.checked=false);e.target.querySelectorAll('select').forEach(n=>n.value='');e.target.dataset.clearBounds='true';};
+$('#filter-form').onsubmit=e=>{e.preventDefault();const data=new FormData(e.target);store.update(s=>{s.search.filters=sanitizeFilters({regions:data.getAll('region'),price:data.get('price'),area:data.get('area'),walk:data.get('walk'),primary:data.get('primary'),junior:data.get('junior'),bounds:e.target.dataset.clearBounds?undefined:s.search.filters.bounds});});$('#filter-dialog').close();searchUpdate({fitBounds:!store.get().search.filters.bounds});router.replace();};
 $('#search-bounds').onclick=()=>{const bounds=map.bounds();if(bounds)store.update(s=>{s.search.filters.bounds=bounds;s.search.boundsPending=false;});searchUpdate();router.replace();};
+$('#map-fit').onclick=()=>{store.update(s=>{s.search.boundsPending=false;});map.fit();$('#search-bounds').hidden=true;router.replace();};
+matchMedia('(min-width:901px)').addEventListener('change',()=>{if(store.get().route.page==='search')searchUpdate({rebuild:false});});
 $('#map-expand').onclick=()=>{map.fullscreen(true);$('#map-close').focus({preventScroll:true});};$('#map-close').onclick=()=>{map.fullscreen(false);$('#map-expand').focus({preventScroll:true});};
 document.addEventListener('keydown',e=>{if(!map.isFull()||document.querySelector('dialog[open]'))return;if(e.key==='Escape'){$('#map-close').click();}if(e.key==='Tab'){const nodes=[...$('#map-shell').querySelectorAll('button,a[href],[tabindex="0"]')].filter(n=>!n.hidden&&n.getBoundingClientRect().height&&getComputedStyle(n).visibility!=='hidden');const first=nodes[0],last=nodes.at(-1);if(e.shiftKey&&document.activeElement===first){e.preventDefault();last?.focus();}else if(!e.shiftKey&&document.activeElement===last){e.preventDefault();first?.focus();}}});
 window.addEventListener('storage',e=>{if(e.key===SAVED_KEY){store.syncSaved(e.newValue);if(store.get().route.page==='saved')refreshContent();updateNav();}});
--- before/assets/land-payment-study/domain.mjs
+++ after/assets/land-payment-study/domain.mjs
@@ -1,9 +1,11 @@
 import {normalizeFinance,flatReduction,calculatePeriods,financeAssumption,financeRevision} from './finance.mjs';
 import {financialProducts,rateValidity} from './data.mjs';
+import {schoolTypes,schoolDistance} from './school.mjs';
 export function selectProperties(items,filters={},sort='price') {
- const list=items.filter(p=>(!filters.regions?.length||filters.regions.includes(p.city??p.name))&&(!filters.price||p.priceMan!=null&&p.priceMan<=filters.price)&&(!filters.area||p.areaM2!=null&&(p.areaMaxM2??p.areaM2)>=filters.area)&&(!filters.walk||p.walkMin!=null&&p.walkStatus!=='conflict'&&p.walkMin<=filters.walk)&&(!filters.bounds||(p.lat>=filters.bounds.south&&p.lat<=filters.bounds.north&&p.lng>=filters.bounds.west&&p.lng<=filters.bounds.east)));
+ const list=items.filter(p=>(!filters.regions?.length||filters.regions.includes(p.city??p.name))&&(!filters.price||p.priceMan!=null&&p.priceMan<=filters.price)&&(!filters.area||p.areaM2!=null&&(p.areaMaxM2??p.areaM2)>=filters.area)&&(!filters.walk||p.walkMin!=null&&p.walkStatus!=='conflict'&&p.walkMin<=filters.walk)&&Object.keys(schoolTypes).every(k=>!filters[k]||schoolDistance(p,k)!=null&&schoolDistance(p,k)<=filters[k])&&(!filters.bounds||(p.lat>=filters.bounds.south&&p.lat<=filters.bounds.north&&p.lng>=filters.bounds.west&&p.lng<=filters.bounds.east)));
  const key={price:'priceMan',area:'areaMaxM2',walk:'walkMin',updated:'sourceUpdatedAt'}[sort]||'priceMan';
- return list.sort((a,b)=>{const x=sort==='area'?(a.areaMaxM2??a.areaM2):a[key],y=sort==='area'?(b.areaMaxM2??b.areaM2):b[key];if(x==null&&y==null)return a.id.localeCompare(b.id);if(x==null)return 1;if(y==null)return-1;return(x===y?0:(x<y?-1:1))*(sort==='area'||sort==='updated'?-1:1)||a.id.localeCompare(b.id);});
+ const value=p=>schoolTypes[sort]?schoolDistance(p,sort):sort==='area'?(p.areaMaxM2??p.areaM2):sort==='walk'&&p.walkStatus==='conflict'?null:p[key];
+ return list.sort((a,b)=>{const x=value(a),y=value(b);if(x==null&&y==null)return a.id.localeCompare(b.id);if(x==null)return 1;if(y==null)return-1;return(x===y?0:(x<y?-1:1))*(sort==='area'||sort==='updated'?-1:1)||a.id.localeCompare(b.id);});
 }
 export function getEstimate(input,productId,today=new Date().toLocaleDateString('sv-SE',{timeZone:'Asia/Tokyo'}),financeSettings={}) {
  const {landMan,buildingMan,cashMan,years}=input,product=financialProducts.find(p=>p.id===productId);
--- before/assets/land-payment-study/map-controller.mjs
+++ after/assets/land-payment-study/map-controller.mjs
@@ -1,17 +1,17 @@
 import {esc,num,price} from './ui.mjs';
 export function createMapController({onSelect,onMove,onCluster}){
- let map,layer,items=[],selectedId=null,programmatic=false,full=false,key='',failed=false;const inertBefore=new Map();
+ let map,layer,items=[],selectedId=null,programmatic=false,full=false,key='',failed=false,autoFit=true;const inertBefore=new Map();
  const shell=document.querySelector('#map-shell'),canvas=document.querySelector('#property-map');
  function init(){
   if(map||failed)return;
   if(!globalThis.L){failed=true;document.querySelector('#map-error').hidden=false;return;}
-  map=L.map(canvas,{zoomControl:false,scrollWheelZoom:false,dragging:innerWidth>900,touchZoom:false,doubleClickZoom:false,boxZoom:false,zoomAnimation:false,fadeAnimation:false,markerZoomAnimation:false}).setView([34.66,135.78],10);
+  map=L.map(canvas,{zoomControl:false,zoomSnap:.25,scrollWheelZoom:false,dragging:innerWidth>900,touchZoom:false,doubleClickZoom:false,boxZoom:false,zoomAnimation:false,fadeAnimation:false,markerZoomAnimation:false}).setView([34.66,135.78],10);
   L.control.zoom({position:'bottomright'}).addTo(map);
   const tiles=L.tileLayer('https://cyberjapandata.gsi.go.jp/xyz/pale/{z}/{x}/{y}.png',{attribution:'<a href="https://maps.gsi.go.jp/development/ichiran.html" target="_blank" rel="noopener">地理院タイル（国土地理院）</a>',maxZoom:18});
   let errors=0;tiles.on('tileerror',()=>{if(++errors>=4)document.querySelector('#map-error').hidden=false;});tiles.on('tileload',()=>{errors=0;document.querySelector('#map-error').hidden=true;});tiles.addTo(map);layer=L.layerGroup().addTo(map);
-  map.on('moveend',()=>{draw();if(!programmatic){const c=map.getCenter();onMove({center:[c.lat,c.lng],zoom:map.getZoom()});}});
+  map.on('moveend',()=>{draw();if(!programmatic){autoFit=false;const c=map.getCenter();onMove({center:[c.lat,c.lng],zoom:map.getZoom()});}});
   canvas.addEventListener('click',e=>{const pin=e.target.closest('[data-pin]'),cluster=e.target.closest('[data-cluster]');if(pin){const id=pin.dataset.pin,onKey=e.detail===0;onSelect(id);if(onKey)canvas.querySelector(`[data-pin="${id}"]`)?.focus({preventScroll:true});}if(cluster)expand(cluster.dataset.cluster.split(','));});
-  const resize=new ResizeObserver(()=>{if(!canvas.clientWidth||!canvas.clientHeight)return;programmatic=true;map.invalidateSize({pan:false});programmatic=false;if(!full){if(innerWidth>900)map.dragging.enable();else map.dragging.disable();}draw();});resize.observe(shell);
+  const resize=new ResizeObserver(()=>{if(!canvas.clientWidth||!canvas.clientHeight)return;programmatic=true;map.invalidateSize({pan:false});programmatic=false;if(!full){if(innerWidth>900)map.dragging.enable();else map.dragging.disable();}if(autoFit)fit();else draw();});resize.observe(shell);
  }
  function draw(){
   if(!map)return;layer.clearLayers();const groups=[];
@@ -21,7 +21,7 @@
   }
  }
  function setView(fn){if(!map)return;programmatic=true;fn();programmatic=false;draw();}
- function fit(){if(items.length)setView(()=>map.fitBounds(items.map(p=>[p.lat,p.lng]),{padding:[54,70],maxZoom:14,animate:false}));}
- function expand(ids){const g=items.filter(x=>ids.includes(x.id));if(!g.length)return;const same=g.every(p=>Math.abs(p.lat-g[0].lat)+Math.abs(p.lng-g[0].lng)<.0001);if(map.getZoom()>=16||same){onCluster(g);return;}const zoom=map.getZoom();setView(()=>{map.fitBounds(g.map(p=>[p.lat,p.lng]),{padding:[70,85],maxZoom:16,animate:false});if(map.getZoom()<=zoom)map.setZoom(Math.min(zoom+2,18),{animate:false});});const c=map.getCenter();onMove({center:[c.lat,c.lng],zoom:map.getZoom()});}
- return {update(next,selection,viewport,{fitBounds=false,restore=false}={}){items=next;selectedId=selection;if(!canvas.clientWidth||!canvas.clientHeight)return;init();if(!map)return;programmatic=true;map.invalidateSize({pan:false});programmatic=false;const nextKey=items.map(x=>x.id).join(',');if(restore&&viewport.center&&viewport.zoom)setView(()=>map.setView(viewport.center,viewport.zoom,{animate:false}));else if(!key||fitBounds)fit();else draw();key=nextKey;},select(id,{pan=false}={}){selectedId=id;if(pan){const p=items.find(x=>x.id===id);if(p)setView(()=>map.setView([p.lat,p.lng],Math.max(map.getZoom(),14),{animate:false}));}draw();},viewport(){if(!map)return {};const c=map.getCenter();return {center:[c.lat,c.lng],zoom:map.getZoom()};},bounds(){if(!map)return null;const b=map.getBounds();return {north:b.getNorth(),south:b.getSouth(),east:b.getEast(),west:b.getWest()};},fullscreen(value){if(full===value)return;full=value;shell.classList.toggle('is-fullscreen',value);document.body.classList.toggle('map-is-fullscreen',value);document.querySelector('#map-close').hidden=!value;document.querySelector('#map-expand').hidden=value;if(value){shell.setAttribute('role','dialog');shell.setAttribute('aria-modal','true');shell.setAttribute('aria-label','土地の地図');for(const el of document.querySelectorAll('.site-header,.preview-label,.search-heading,.search-toolbar,#active-filters,.list-panel,#selected-card,.map-footnote,.catalog-note,#content-screen,#compare-tray')){inertBefore.set(el,el.inert);el.inert=true;}}else{shell.removeAttribute('role');shell.removeAttribute('aria-modal');shell.removeAttribute('aria-label');for(const [el,was]of inertBefore)el.inert=was;inertBefore.clear();}if(map){if(value||innerWidth>900)map.dragging.enable();else map.dragging.disable();value?map.touchZoom.enable():map.touchZoom.disable();programmatic=true;map.invalidateSize({pan:false});programmatic=false;draw();}},isFull:()=>full};
+ function fit(){autoFit=true;if(items.length)setView(()=>map.fitBounds(items.map(p=>[p.lat,p.lng]),{padding:[54,48],maxZoom:14,animate:false}));else draw();}
+ function expand(ids){autoFit=false;const g=items.filter(x=>ids.includes(x.id));if(!g.length)return;const same=g.every(p=>Math.abs(p.lat-g[0].lat)+Math.abs(p.lng-g[0].lng)<.0001);if(map.getZoom()>=16||same){onCluster(g);return;}const zoom=map.getZoom();setView(()=>{map.fitBounds(g.map(p=>[p.lat,p.lng]),{padding:[70,85],maxZoom:16,animate:false});if(map.getZoom()<=zoom)map.setZoom(Math.min(zoom+2,18),{animate:false});});const c=map.getCenter();onMove({center:[c.lat,c.lng],zoom:map.getZoom()});}
+ return {update(next,selection,viewport,{fitBounds=false,restore=false}={}){items=next;selectedId=selection;if(!canvas.clientWidth||!canvas.clientHeight)return;init();if(!map)return;programmatic=true;map.invalidateSize({pan:false});programmatic=false;const nextKey=items.map(x=>x.id).join(',');if(restore&&viewport.center&&viewport.zoom){autoFit=false;setView(()=>map.setView(viewport.center,viewport.zoom,{animate:false}));}else if(!key||fitBounds)fit();else draw();key=nextKey;},select(id,{pan=false}={}){selectedId=id;if(pan){autoFit=false;const p=items.find(x=>x.id===id);if(p)setView(()=>map.setView([p.lat,p.lng],Math.max(map.getZoom(),14),{animate:false}));}draw();},viewport(){if(!map)return {};const c=map.getCenter();return {center:[c.lat,c.lng],zoom:map.getZoom()};},bounds(){if(!map)return null;const b=map.getBounds();return {north:b.getNorth(),south:b.getSouth(),east:b.getEast(),west:b.getWest()};},fullscreen(value){if(full===value)return;full=value;shell.classList.toggle('is-fullscreen',value);document.body.classList.toggle('map-is-fullscreen',value);document.querySelector('#map-close').hidden=!value;document.querySelector('#map-expand').hidden=value;if(value){shell.setAttribute('role','dialog');shell.setAttribute('aria-modal','true');shell.setAttribute('aria-label','土地の地図');for(const el of document.querySelectorAll('.site-header,.preview-label,.search-heading,.search-toolbar,#active-filters,.list-panel,#selected-card,.map-footnote,.catalog-note,#content-screen,#compare-tray')){inertBefore.set(el,el.inert);el.inert=true;}}else{shell.removeAttribute('role');shell.removeAttribute('aria-modal');shell.removeAttribute('aria-label');for(const [el,was]of inertBefore)el.inert=was;inertBefore.clear();}if(map){if(value||innerWidth>900)map.dragging.enable();else map.dragging.disable();value?map.touchZoom.enable():map.touchZoom.disable();programmatic=true;map.invalidateSize({pan:false});programmatic=false;draw();}},fit,isFull:()=>full};
 }
--- before/assets/land-payment-study/router.mjs
+++ after/assets/land-payment-study/router.mjs
@@ -1,3 +1,4 @@
+import {searchSorts} from './school.mjs';
 import {property,validIds} from './catalog.mjs';
 import {sanitizeFilters,createDraft} from './state.mjs';
 export function parseRoute(hash){
@@ -9,7 +10,7 @@
  if(page==='compare')return {page,ids:validIds(new URLSearchParams(query).get('ids')?.split(','))};
  if(['saved','inquiry'].includes(page))return {page};
  if(page==='search'){
-  const q=new URLSearchParams(query),f={regions:q.getAll('region')};for(const k of ['price','area','walk'])f[k]=q.get(k);
+  const q=new URLSearchParams(query),f={regions:q.getAll('region')};for(const k of ['price','area','walk','primary','junior'])f[k]=q.get(k);
   const b=q.get('bounds')?.split(',').map(Number);if(b?.length===4)f.bounds={north:b[0],south:b[1],east:b[2],west:b[3]};
   return {page:'search',filters:sanitizeFilters(f),sort:q.get('sort')||'price',view:q.get('view')||'map',fromUrl:true};
  }
@@ -17,7 +18,7 @@
 }
 export function routeHash(route,state){
  if(route.page==='search'){
-  const q=new URLSearchParams(),s=state.search;for(const r of s.filters.regions)q.append('region',r);for(const k of ['price','area','walk'])if(s.filters[k])q.set(k,s.filters[k]);
+  const q=new URLSearchParams(),s=state.search;for(const r of s.filters.regions)q.append('region',r);for(const k of ['price','area','walk','primary','junior'])if(s.filters[k])q.set(k,s.filters[k]);
   if(s.filters.bounds){const b=s.filters.bounds;q.set('bounds',[b.north,b.south,b.east,b.west].map(v=>Number(v.toFixed(6))).join(','));}
   if(s.sort!=='price')q.set('sort',s.sort);if(s.view==='list')q.set('view','list');return '#/search'+(q.size?'?'+q:'');
  }
@@ -25,17 +26,26 @@
  return '#/'+route.page+(route.id?'/'+encodeURIComponent(route.id):'');
 }
 export function createRouter(store,{render,capture,onMissing=()=>{}}){
+ // Search history already restores its recorded position in render().
+ history.scrollRestoration='manual';
  function safeRoute(r){if(r.page==='estimate'&&!r.id){let d;store.update(s=>{d=createDraft(s);});return {...r,id:d.id};}if(r.page==='property'&&!property(r.id)){onMissing('この物件は現在掲載されていません。');return {page:'search'};}if(['estimate','result'].includes(r.page)&&!store.get().drafts[r.id]){onMissing('この端末には計算条件がありません。「予算から計算」からご入力ください。');return {page:'search'};}return r;}
  function saveEntry(){capture();history.replaceState({yamato:2,snapshot:store.snapshot()},'',routeHash(store.get().route,store.get()));}
- function go(route,{replace=false,restored=false}={}){
+ function go(route,{replace=false,restored=false,fitBounds=false}={}){
   if(!restored)saveEntry();route=safeRoute(route);store.update(s=>{s.route=route;if(route.page==='property')s.search.selectedId=route.id;});
   if(!restored)history[replace?'replaceState':'pushState']({yamato:2,snapshot:store.snapshot()},'',routeHash(route,store.get()));
-  render(route,{restored});
+  render(route,{restored,fitBounds});
  }
- const pop=e=>{if(e.state?.yamato===2)store.restore(e.state.snapshot);go(parseRoute(location.hash),{restored:true});};
+ function applySearchUrl(route){
+  if(route.page!=='search'||!route.fromUrl)return false;
+  const filterHash=filters=>routeHash({page:'search'},{search:{filters,sort:'price',view:'map'}});
+  const changed=filterHash(store.get().search.filters)!==filterHash(route.filters);
+  store.update(s=>{s.search.filters=route.filters;s.search.sort=searchSorts.includes(route.sort)?route.sort:'price';s.search.view=route.view==='list'?'list':'map';if(changed)Object.assign(s.search,{selectedId:null,center:null,zoom:null,scroll:0,listScroll:0,boundsPending:false});});
+  return changed;
+ }
+ const pop=e=>{if(e.state?.yamato===2)store.restore(e.state.snapshot);const route=parseRoute(location.hash),fitBounds=applySearchUrl(route);go(route,{restored:true,fitBounds});};
  window.addEventListener('popstate',pop);
  let route=parseRoute(location.hash);
- if(route.page==='search'&&route.fromUrl)store.update(s=>{s.search.filters=route.filters;s.search.sort=['price','area','walk','updated','region'].includes(route.sort)?route.sort:'price';s.search.view=route.view==='list'?'list':'map';});
+ applySearchUrl(route);
  if(route.page==='compare'&&route.ids.length)store.update(s=>{s.compareIds=route.ids.slice(0,3);});
  route=safeRoute(route);store.update(s=>{s.route=route;});history.replaceState({yamato:2,snapshot:store.snapshot()},'',routeHash(route,store.get()));
  window.addEventListener('pagehide',saveEntry);
--- before/assets/land-payment-study/state.mjs
+++ after/assets/land-payment-study/state.mjs
@@ -1,4 +1,5 @@
 import {financeDefaults,normalizeFinance} from './finance.mjs';
+import {schoolLimits,searchSorts} from './school.mjs';
 import {property,validIds,cities,houses,financialProducts,catalogRevision} from './catalog.mjs';
 export const SESSION_KEY='yamato-land-study:session:v2',SAVED_KEY='yamato-land-study:saved:v2';
 const copy=v=>structuredClone(v);
@@ -6,7 +7,7 @@
 const number=(v,min,max,fallback)=>v!==''&&Number.isFinite(Number(v))&&Number(v)>=min&&Number(v)<=max?Number(v):fallback;
 export function sanitizeFilters(value={}){
  const f={regions:[...new Set(Array.isArray(value.regions)?value.regions:[])].filter(x=>cities.includes(x))};
- for(const [k,allowed] of Object.entries({price:[1000,1500,2000,2500,3000],area:[150,180],walk:[10,15,20]}))if(allowed.includes(Number(value[k])))f[k]=Number(value[k]);
+ for(const [k,allowed] of Object.entries({price:[1000,1500,2000,2500,3000],area:[150,180],walk:[10,15,20],primary:schoolLimits,junior:schoolLimits}))if(allowed.includes(Number(value[k])))f[k]=Number(value[k]);
  const b=value.bounds;
  if(b&&['north','south','east','west'].every(k=>Number.isFinite(Number(b[k])))&&b.north>=b.south&&b.east>=b.west&&Math.abs(b.north)<=90&&Math.abs(b.south)<=90&&Math.abs(b.east)<=180&&Math.abs(b.west)<=180)f.bounds=Object.fromEntries(Object.entries(b).filter(([k])=>['north','south','east','west'].includes(k)).map(([k,v])=>[k,Number(v)]));
  return f;
@@ -16,7 +17,8 @@
  const s=defaults();if(!raw||raw.version!==2)return s;
  s.conditionsEpoch=number(raw.conditionsEpoch,0,Number.MAX_SAFE_INTEGER,0);s.common=normalizeCommon(raw.common);s.search.filters=sanitizeFilters(raw.search?.filters);
  s.search.view=raw.search?.view==='list'?'list':'map';s.search.selectedId=property(raw.search?.selectedId)?.id??null;
- s.search.sort=['price','area','walk','updated','region'].includes(raw.search?.sort)?raw.search.sort:'price';
+ s.search.sort=searchSorts.includes(raw.search?.sort)?raw.search.sort:'price';
+ s.search.boundsPending=!!raw.search?.boundsPending;
  if(Array.isArray(raw.search?.center)&&raw.search.center.length===2&&raw.search.center.every(Number.isFinite)&&Math.abs(raw.search.center[0])<=90&&Math.abs(raw.search.center[1])<=180)s.search.center=raw.search.center;
  s.search.zoom=number(raw.search?.zoom,3,18,null);s.search.scroll=number(raw.search?.scroll,0,1000000,0);s.search.listScroll=number(raw.search?.listScroll,0,1000000,0);
  s.compareIds=validIds(raw.compareIds).slice(0,3);
--- before/assets/land-payment-study/style.css
+++ after/assets/land-payment-study/style.css
@@ -13,3 +13,17 @@
 /* Rate assumptions stay beside the amount; optional inputs remain progressive. */
 .payment-term{font-size:15px;font-weight:600;color:var(--green);margin:0 0 16px}.finance-basis{background:#eceee7;border-left:3px solid #6b806d;padding:17px 18px;margin:22px 0}.finance-basis strong{font-size:15px}.finance-basis p{font-size:13px;line-height:1.85;margin:9px 0 0}.finance-basis .basis-date{font-size:12px;color:#56655b}.repayment-periods{margin:28px 0}.repayment-periods h2{font-size:18px;margin-bottom:16px}.repayment-periods table{border-collapse:collapse;width:100%;font-variant-numeric:tabular-nums}.repayment-periods th,.repayment-periods td{padding:13px 9px;border-bottom:1px solid #d4ddd3;text-align:left;font-size:13px;white-space:nowrap}.repayment-periods thead th{font-size:12px;font-weight:500;color:#536357}.repayment-periods td:last-child{text-align:right}.repayment-periods td strong{font-size:20px;font-weight:600}.repayment-periods td small{margin-left:3px;font-size:11px}.finance-settings{border:1px solid #c9d4ca;border-radius:10px;padding:16px 18px;margin:22px 0}.finance-settings summary{cursor:pointer;font-size:15px;font-weight:600;min-height:28px}.finance-settings form{margin-top:18px}.finance-fields .field{margin:20px 0 10px;display:block}.finance-fields .field input,.finance-fields .field select{display:block;width:100%;margin-top:8px;min-height:48px}.finance-fields .note{font-size:12px;line-height:1.85}.finance-fields[data-flat-mode=standard] .flat-scenario{display:none}.flat-scenario{border-left:2px solid #c8d1c5;padding-left:16px}.flat-scenario>a{display:block;font-size:13px;margin:12px 0}.cash-summary{border:1px solid #b5c8b7;background:#edf1ea;padding:22px 18px;margin-top:28px;border-radius:10px}.cash-summary h2{font-size:18px;margin:0 0 12px}.cash-total{font-size:30px;letter-spacing:-.03em;margin:0 0 14px}.cost-checks{margin:28px 0}.cost-checks h2{font-size:16px}.cost-checks ul{padding-left:20px}.cost-checks li{font-size:13px;line-height:1.9;margin:10px 0}.later-payment{font-size:13px;line-height:1.8;margin:12px 0;padding-top:10px;border-top:1px solid #d4ddd3}.inquiry-estimate .repayment-periods{max-width:520px}.finance-button .finance-payment small{line-height:1.7}
 @media(max-width:370px){.repayment-periods th,.repayment-periods td{padding:12px 4px;font-size:12px}.repayment-periods td strong{font-size:18px}.finance-settings{padding:14px}.flat-scenario{padding-left:10px}.finance-button .finance-payment{font-size:21px}}
+/* Search map and results share one normal document flow on desktop. */
+.results-jump{display:none}.results-heading{display:none}.card-schools{margin:14px 0 0;padding-top:12px;border-top:1px solid var(--line);font-size:13px;line-height:1.65}.card-schools>div{display:grid;grid-template-columns:46px minmax(0,1fr);gap:9px;margin-top:5px}.card-schools dt{color:var(--muted)}.card-schools dd{margin:0;display:flex;justify-content:space-between;align-items:baseline;gap:10px}.card-schools dd span{min-width:0;overflow-wrap:anywhere}.card-schools strong{font-weight:500;white-space:nowrap}.school-source{font-size:12px;color:var(--muted);margin-top:6px}.map-fit{position:absolute;z-index:500;left:16px;bottom:18px;min-height:44px;font-size:13px;box-shadow:0 2px 8px #213c311a}.school-filters{border-block:1px solid var(--line);padding:14px 0;margin-top:20px}.school-filters summary{cursor:pointer;min-height:44px;display:flex;align-items:center;justify-content:space-between;font-size:16px;font-weight:600;list-style:none}.school-filters summary::-webkit-details-marker{display:none}.school-filters summary::after{content:'＋';font-size:20px;font-weight:400}.school-filters[open] summary::after{content:'−'}.school-filters .note{margin:4px 0 16px}.school-filter-fields{display:grid;grid-template-columns:1fr 1fr;gap:16px}.school-filter-fields label{display:grid;gap:8px}.school-filter-fields select{width:100%}#property-results{scroll-margin-top:24px}
+@media(min-width:901px){
+ .search-heading{padding-top:22px;padding-bottom:16px}.search-toolbar .view-switch{display:none}.results-jump{display:inline-flex;align-items:center;min-height:48px;margin-left:auto;font-size:14px;text-decoration:underline;text-underline-offset:5px}
+ .workspace,.workspace.list-mode{display:flex;flex-direction:column;gap:28px;margin:0 4%;padding:0;border:0;max-width:none}
+ .workspace .map-column,.workspace.list-mode .map-column{display:block;width:100%;position:static}.workspace .map-shell:not(.is-fullscreen){height:clamp(300px,calc(100svh - 365px),520px);min-height:0;border-radius:12px;overflow:hidden}
+ .workspace .list-panel,.workspace.list-mode .list-panel{display:block;width:100%;height:auto;min-height:0;max-height:none;overflow:visible;overscroll-behavior:auto;padding:0;background:none}
+ .workspace #list,.workspace.list-mode #list{display:grid;grid-template-columns:repeat(3,minmax(0,1fr));gap:32px}.workspace .property-card{margin:0 0 12px;padding-bottom:28px}.workspace .card-picture{aspect-ratio:1.6}.workspace .map-footnote{padding:10px 2px 0;font-size:12px}
+ .results-heading{display:flex;align-items:center;justify-content:space-between;gap:24px;margin-bottom:16px}.results-heading h2{font-size:24px}.results-tools .sort-label{font-size:13px}.results-tools .sort-label select{max-width:260px;font-size:14px}.list-note{margin-bottom:8px}#match-note{margin-bottom:22px}.map-expand{display:block}.map-shell.is-fullscreen .map-expand{display:none}.selected-card{border:1px solid var(--line);border-radius:10px;margin-top:14px;max-width:680px}.catalog-note{padding-top:24px}
+}
+@media(min-width:901px) and (max-width:1100px){.workspace #list,.workspace.list-mode #list{grid-template-columns:repeat(2,minmax(0,1fr));gap:26px}}
+@media(max-width:900px){.map-fit{left:12px;bottom:14px;font-size:12px}.workspace.list-mode #school-note{margin-bottom:8px}.workspace.list-mode #match-note{margin-bottom:20px}}
+@media(max-width:370px){.school-filter-fields{grid-template-columns:1fr}.card-schools{font-size:13px}.map-fit{padding-inline:12px}}
+.map-error,.map-empty{z-index:650}.map-shell:has(.map-error:not([hidden]),.map-empty:not([hidden])) .map-fit{display:none}
--- before/assets/land-payment-study/views/search.mjs
+++ after/assets/land-payment-study/views/search.mjs
@@ -1,17 +1,28 @@
+import {schoolTypes,listedSchool} from '../school.mjs';
 import {queryCatalog,property,cities} from '../catalog.mjs';
 import {esc,num,price,area,photo,station,savedButton,icon} from '../ui.mjs';
+export function schoolFacts(p){
+ return `<dl class="card-schools">${Object.entries(schoolTypes).map(([key,label])=>{const a=listedSchool(p,key);return `<div><dt>${label}</dt><dd>${a?`<span>${esc(a.name)}</span><strong>${num(a.publishedMeters)}m</strong>`:'<span>距離の情報なし</span>'}</dd></div>`;}).join('')}</dl>`;
+}
+export function syncSearchLayout(){
+ const sort=document.querySelector('.sort-label'),desktop=matchMedia('(min-width:901px)').matches;
+ const target=document.querySelector(desktop?'.results-tools':'.search-toolbar');
+ if(sort.parentElement!==target)target.append(sort);
+}
 export function propertyCard(p,store,{compare=false,unverified=false}={}){
  const selected=store.get().search.selectedId===p.id;
- return `<article class="property-card ${selected?'is-selected':''}" data-id="${p.id}"><div class="card-picture"><a href="#/property/${p.id}" data-property="${p.id}" tabindex="-1" aria-hidden="true">${photo(p)}</a>${savedButton(p,store.has(p.id),true)}</div><div class="card-body"><p class="card-city">${esc(p.city)}<span>土地</span></p><h2><a href="#/property/${p.id}" data-property="${p.id}">${esc(p.name)}</a></h2><p class="card-price">${price(p)}<small>万円</small></p><p class="card-facts">${area(p)}㎡<span>${(p.areaM2/3.305785).toFixed(1)}${p.areaMaxM2>p.areaM2?'〜':''}坪</span></p><p class="card-access">${station(p)}<small>SUUMO掲載</small></p>${unverified?'<p class="status-note">ご希望の価格・面積に合う区画は要確認</p>':''}<div class="card-actions"><button data-map-select="${p.id}">${icon('map')}地図</button><a class="detail-link" href="#/property/${p.id}" data-property="${p.id}">詳細を見る ${icon('arrow')}</a></div>${compare?`<label class="compare-choice"><input type="checkbox" data-compare="${p.id}" ${store.get().compareIds.includes(p.id)?'checked':''}>比較に追加</label>`:''}</div></article>`;
+ return `<article class="property-card ${selected?'is-selected':''}" data-id="${p.id}"><div class="card-picture"><a href="#/property/${p.id}" data-property="${p.id}" tabindex="-1" aria-hidden="true">${photo(p)}</a>${savedButton(p,store.has(p.id),true)}</div><div class="card-body"><p class="card-city">${esc(p.city)}<span>土地</span></p><h2><a href="#/property/${p.id}" data-property="${p.id}">${esc(p.name)}</a></h2><p class="card-price">${price(p)}<small>万円</small></p><p class="card-facts">${area(p)}㎡<span>${(p.areaM2/3.305785).toFixed(1)}${p.areaMaxM2>p.areaM2?'〜':''}坪</span></p><p class="card-access">${station(p)}<small>SUUMO掲載</small></p>${schoolFacts(p)}${compare?'<p class="school-source">SUUMO掲載・通学区域は要確認</p>':''}${unverified?'<p class="status-note">ご希望の価格・面積に合う区画は要確認</p>':''}<div class="card-actions"><button data-map-select="${p.id}">${icon('map')}地図</button><a class="detail-link" href="#/property/${p.id}" data-property="${p.id}">詳細を見る ${icon('arrow')}</a></div>${compare?`<label class="compare-choice"><input type="checkbox" data-compare="${p.id}" ${store.get().compareIds.includes(p.id)?'checked':''}>比較に追加</label>`:''}</div></article>`;
 }
 export function renderSearch(store,{rebuild=true}={}){
+ syncSearchLayout();
  const s=store.get().search,rows=queryCatalog(s.filters,s.sort),list=document.querySelector('#list');
  document.querySelector('#workspace').classList.toggle('list-mode',s.view==='list');document.querySelector('#map-view').setAttribute('aria-pressed',String(s.view==='map'));document.querySelector('#list-view').setAttribute('aria-pressed',String(s.view==='list'));document.querySelector('#sort').value=s.sort;
  document.querySelector('#count').textContent=`${rows.length}件`;
  document.querySelector('#map-empty').hidden=rows.length!==0;
  const keys=Object.keys(s.filters).filter(k=>k==='regions'?s.filters.regions.length:s.filters[k]);document.querySelector('#filter-count').textContent=keys.length||'';
- document.querySelector('#match-note').textContent=s.sort==='updated'?'情報提供日はすべて2026年9月6日です。':rows.some(r=>r.matchStatus==='plot-unverified')?'価格や面積に幅がある物件を含みます。ご希望に合う区画は担当者にご確認ください。':'価格・面積・徒歩時間はSUUMOの掲載情報です。';
- document.querySelector('#active-filters').innerHTML=keys.map(k=>`<button data-remove-filter="${k}">${k==='regions'?esc(s.filters.regions.join('・')):k==='price'?`${num(s.filters.price)}万円まで`:k==='area'?`${s.filters.area}㎡以上`:k==='walk'?`駅まで徒歩${s.filters.walk}分以内`:'地図の範囲'} <span aria-hidden="true">×</span></button>`).join('')+(keys.length?'<button class="clear-all" data-reset-filters>すべて解除</button>':'');
+ const schoolNote=document.querySelector('#school-note');schoolNote.hidden=!rows.length;schoolNote.textContent='学校までの距離はSUUMOの掲載情報です。通う学校・通学路は未確認です。';
+ document.querySelector('#match-note').textContent=schoolTypes[s.sort]?`${schoolTypes[s.sort]}までの掲載距離が短い順です。距離の情報がない物件は最後に表示します。`:s.sort==='updated'?'情報提供日はすべて2026年9月6日です。':rows.some(r=>r.matchStatus==='plot-unverified')?'価格や面積に幅がある物件を含みます。ご希望に合う区画は担当者にご確認ください。':'価格・面積・徒歩時間はSUUMOの掲載情報です。';
+ document.querySelector('#active-filters').innerHTML=keys.map(k=>`<button data-remove-filter="${k}">${k==='regions'?esc(s.filters.regions.join('・')):k==='price'?`${num(s.filters.price)}万円まで`:k==='area'?`${s.filters.area}㎡以上`:k==='walk'?`駅まで徒歩${s.filters.walk}分以内`:schoolTypes[k]?`${schoolTypes[k]}まで${num(s.filters[k])}m以内`:'地図の範囲'} <span aria-hidden="true">×</span></button>`).join('')+(keys.length?'<button class="clear-all" data-reset-filters>すべて解除</button>':'');
  if(rebuild){const scroll=document.querySelector('.list-panel').scrollTop;list.innerHTML=rows.length?rows.map(r=>propertyCard(r.property,store,{unverified:r.matchStatus==='plot-unverified'})).join(''):'<div class="empty-state"><h2>条件に合う物件が見つかりません</h2><p>地域や予算を変えて、もう一度お探しください。</p><button data-reset-filters class="primary">絞り込みを解除</button></div>';document.querySelector('.list-panel').scrollTop=scroll;}
  renderSelection(store);return rows.map(r=>r.property);
 }
@@ -20,4 +31,4 @@
  node.innerHTML=`<div class="selected-photo">${photo(p,'',true)}</div><div class="selected-body"><p class="eyebrow">選択中 · ${esc(p.city)}</p><h2>${esc(p.name)}</h2><p class="selected-price">${price(p)}<small>万円</small></p><p class="note">土地 ${area(p)}㎡</p><div class="selected-actions"><a href="#/property/${p.id}" data-property="${p.id}" class="primary">詳細を見る ${icon('arrow')}</a>${savedButton(p,store.has(p.id),true)}</div></div><button id="close-card" class="selection-close" aria-label="選択した物件を閉じる">${icon('close')}</button>`;
  document.querySelectorAll('.property-card').forEach(c=>c.classList.toggle('is-selected',c.dataset.id===p.id));
 }
-export function fillFilters(store){const form=document.querySelector('#filter-form'),f=store.get().search.filters;document.querySelector('#region-options').innerHTML=cities.map(city=>`<label><input type="checkbox" name="region" value="${esc(city)}" ${f.regions.includes(city)?'checked':''}>${esc(city)}</label>`).join('');for(const k of ['price','area','walk'])form.elements[k].value=f[k]??'';}
+export function fillFilters(store){const form=document.querySelector('#filter-form'),f=store.get().search.filters;document.querySelector('#region-options').innerHTML=cities.map(city=>`<label><input type="checkbox" name="region" value="${esc(city)}" ${f.regions.includes(city)?'checked':''}>${esc(city)}</label>`).join('');for(const k of ['price','area','walk','primary','junior'])form.elements[k].value=f[k]??'';document.querySelector('#school-filters').open=!!(f.primary||f.junior);delete form.dataset.clearBounds;}
--- before/assets/land-payment-study/views/compare.mjs
+++ after/assets/land-payment-study/views/compare.mjs
@@ -7,7 +7,7 @@
  if(items.length<2)return `<div class="content-wrap"><a href="#/saved" data-page="saved" class="back-link">← 保存した土地に戻る</a><h1 tabindex="-1">土地を比較する</h1><div class="empty-state"><h2>比較する土地を2〜3件選んでください</h2><p>保存した土地から候補を選べます。</p><a class="primary" href="#/saved" data-page="saved">保存した土地を見る</a></div></div>`;
  const outcomes=items.map(p=>estimateFor(p.id,c,s.budgets[p.id]??p.priceMan));let rowIndex=0;
  const row=(label,values,cls='')=>`<section class="comparison-row ${cls}" aria-labelledby="compare-row-${++rowIndex}"><h2 id="compare-row-${rowIndex}">${label}</h2><div class="comparison-values" style="--columns:${items.length}">${values.map((value,i)=>`<div><p class="comparison-property">${esc(items[i].name)}</p>${value}</div>`).join('')}</div></section>`;
- const facility=(p,type)=>{const a=p.amenities.find(x=>x.type===type&&x.name);return a?`<p>${esc(a.name)}</p><p class="note">${a.publishedMinutes!=null?'徒歩'+a.publishedMinutes+'分（SUUMO掲載）':'徒歩時間の情報なし'}${type.includes('学校')?'<br>通学区域は要確認':''}</p>`:'<p class="note">情報なし</p>';};
+ const facility=(p,type)=>{const a=p.amenities.find(x=>x.type===type&&x.name);return a?`<p>${esc(a.name)}</p><p class="note">${a.publishedMeters!=null?num(a.publishedMeters)+'m ／ ':''}${a.publishedMinutes!=null?'徒歩'+a.publishedMinutes+'分（SUUMO掲載）':'徒歩時間の情報なし'}${type.includes('学校')?'<br>通学区域は要確認':''}</p>`:'<p class="note">情報なし</p>';};
  return `<div class="content-wrap comparison-page"><a href="#/saved" data-page="saved" class="back-link">← 保存した土地に戻る</a><h1 tabindex="-1">${items.length}件の土地を比較する</h1><p class="page-lead">同じ建物プラン・頭金・返済期間で、返済額と周辺環境を比較できます。</p><details class="common-settings" ${house?'':'open'}><summary>計算条件・通勤先を変更 <span>${house?`${house.name} ／ 頭金${num(c.cashMan)}万円 ／ ${c.years}年`:'建物プランを選んでください'}</span></summary><form id="compare-form">${commonFields(c,{destinations:true})}<label class="field">住宅ローン<select name="financeId">${financialProducts.map(p=>`<option value="${p.id}" ${p.id===c.financeId?'selected':''}>${p.bank} ${p.name}</option>`).join('')}</select></label><details class="finance-settings"><summary>金利・諸費用を詳しく設定</summary>${financeFields(c.finance)}</details><p class="note">通勤先は相談時の確認用に保存します。経路や所要時間の自動計算には対応していません。</p><button class="primary" type="submit">この条件で比較する</button></form></details><p class="comparison-assumptions">${house?`建物「${house.name}」・頭金${num(c.cashMan)}万円・${c.years}年／${finance.bank} ${finance.name}`:'建物プランを選ぶと毎月の返済額が表示されます。'}<br>土地は入力した予算で計算します。元利均等返済・ボーナス返済なし。諸費用は別途必要です。</p><div class="comparison-head" style="--columns:${items.length}">${items.map(p=>`<article>${photo(p)}<p class="eyebrow">${esc(p.city)}</p><h2><a href="#/property/${p.id}" data-property="${p.id}">${esc(p.name)}</a></h2><button data-remove-compare="${p.id}" aria-label="${esc(p.name)}を比較から外す">比較から外す ×</button></article>`).join('')}</div>
  ${row('土地の掲載価格',items.map(p=>`<p class="comparison-price">${price(p)}<small>万円</small></p><p class="note">販売中の区画・価格は要確認</p>`))}
  ${row('計算に使う土地予算',items.map(p=>`<label class="budget-field"><span class="sr-only">${esc(p.name)}の土地予算</span><input type="number" inputmode="numeric" min="0" max="10000" step="1" data-budget="${p.id}" value="${esc(s.budgets[p.id]??p.priceMan)}">万円</label><p class="note">${s.budgets[p.id]==null?'掲載価格の下限で計算':'入力した予算で計算'}</p>`))}
--- before/assets/land-payment-study/school.mjs
+++ after/assets/land-payment-study/school.mjs
@@ -0,0 +1,12 @@
+export const schoolTypes={primary:'小学校',junior:'中学校'};
+export const schoolLimits=[800,1200,1600];
+export const searchSorts=['price','area','walk','primary','junior','updated','region'];
+
+// Compare published distances only; coordinates do not establish school routes.
+export function listedSchool(p,key){
+ const type=schoolTypes[key];
+ if(!type)return null;
+ return (p.amenities??[]).filter(a=>a.type===type&&a.name&&a.source===p.source&&typeof a.publishedMeters==='number'&&Number.isFinite(a.publishedMeters)&&a.publishedMeters>0)
+  .sort((a,b)=>a.publishedMeters-b.publishedMeters||a.name.localeCompare(b.name,'ja'))[0]??null;
+}
+export const schoolDistance=(p,key)=>listedSchool(p,key)?.publishedMeters??null;
--- before/scripts/package-land-payment.mjs
+++ after/scripts/package-land-payment.mjs
@@ -4,8 +4,8 @@
 import {tmpdir} from 'node:os';
 import {createHash} from 'node:crypto';

-const snapshot = process.argv[2] || '20260908';
-if (!/^[0-9]{8}$/.test(snapshot)) throw new Error('Use a snapshot date: YYYYMMDD');
+const snapshot = process.argv[2] || '20260908-pc';
+if (!/^[0-9]{8}(?:-[a-z0-9]+)?$/.test(snapshot)) throw new Error('Use a snapshot version: YYYYMMDD or YYYYMMDD-name');
 const root = fileURLToPath(new URL(`../snapshots/land-payment-${snapshot}/`, import.meta.url));
 const manifest = JSON.parse(await readFile(join(root, 'manifest.json'), 'utf8'));
 const stage = await mkdtemp(join(tmpdir(), 'yamato-land-payment-'));
