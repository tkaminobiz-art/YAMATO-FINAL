# Integrated interaction review

PASS on actual Chromium and WebKit, both 390 and 1440: correct silent inline PC/SP H.264 video; pause keeps currentTime stable; restart advances it; offscreen stops it; return resumes; approximately 10 seconds plus end hold leads into scenes 03–08, and 08 remains stopped. Manual dusk then resume goes to 03 rather than jumping back to daylight. Each of the eight manual scenes loads, and the CTA opens canonical kodawari.html.

Reduced-motion: zero MP4 requests and all scenes remain manually accessible. Save-data: zero MP4 requests, no automatic playback and manual dusk succeeds. Rejected play and aborted MP4 retain an image and usable manual navigation. No page errors.

The canonical integration replaces the legacy hero script, not adds a second clock. Preview-only model comparison/status code was removed, URLs resolve through six explicit approved runtime assets. The public generation disclosure and all below-FV content remain unchanged. These tests are browser emulation, not physical iPhone certification.
