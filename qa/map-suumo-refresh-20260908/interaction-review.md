# Interaction review

ローカル配布物をChromiumでPC 1440pxとスマートフォン390pxとして確認した。28項目PASS、コンソールエラー0件。

- 検索・地図・一覧が40件で開く。
- 「新しい順」で9月8日の23件、9月7日の17件の順に並び、補足文が `情報提供日が新しい順です。` になる。
- 保存済み `s21320158` を保持し、詳細・比較・試算へ進める。
- 旧 `suumo-20260907-v1` の計算結果は、入力を保持して再計算を案内する。
- 新しい版の相談内容は検証を通り、旧版の相談内容は受信前検証で止まる。
- 金融商品切替、TOP導線、PC地図の非追従を保持する。
- 実際の問い合わせ送信は行わない。

記録：`local-browser.json`。代表画面は `local-pc-1440-search.png`、`local-pc-1440-result.png`、`local-sp-390-search.png`、`local-sp-390-result.png`。
