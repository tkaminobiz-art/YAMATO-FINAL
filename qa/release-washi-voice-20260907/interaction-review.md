# Integration verification

local-browser-report.json: eight engine/width configurations. Nara origin controls switch minutes and source disclosure; FAQ open/close; contact information dialog open/Escape (no real send); reduced-motion rail remains static, fresh normal-motion visit autostarts, deliberate touch pauses, next control moves; parallax photos cover their frames.

Chromium and WebKit, PC1440 and SP390: all four voice cards open existing voice.html#v01/#v02/#v08/#v33 response modals and Escape works; list link shows 50 existing entries. Section has no new autoplay or custom JS. Four original excerpts and photo disclaimers retained.

gallery-report.json: four Chromium/WebKit PC/SP fixture runs, 10 images decoded, gallery modal/next/Escape passed. Fixtures are explicitly QA-generated data, not live Instagram claims. The main suite also exercises safe API-unavailable display. Production API must be checked separately after release, with no token reads or environment edits.

protection-report.json: original FV byte-identical; v1top, Works, Kodawari, response data, hero dependencies and Instagram server code unchanged. Homepage regression self-test rejects seven representative regressions. Runtime packaging contains 602 files and no staff/FV_Bright/3D prototype paths. Review evidence gate is a record-consistency check, not aesthetic approval.

Existing contact form submission is still not connected; noindex/noarchive release hold is unchanged. This deployment publishes the requested visual updates without representing either as resolved.
