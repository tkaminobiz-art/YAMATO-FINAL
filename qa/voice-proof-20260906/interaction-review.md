# 操作・読み上げ確認

- 個別導線 `voice.html#v01`、`voice.html#v08`、`voice.html#v33` と一覧導線 `voice.html` を保持。
- `voice.html#v33` で対応する詳細ダイアログと原文が表示されることをローカル実操作で確認。
- 各リンクはネイティブのアンカーで、キーボードフォーカスのアウトラインを保持。
- 自動再生、自動展開、新規のJavaScript操作は追加していない。`prefers-reduced-motion` で残るhoverトランジションを無効化。
- 見出しと引用はHTMLテキストのため、CSSと外部フォントが読み込めない場合も内容は読める。
