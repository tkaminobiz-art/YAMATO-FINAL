# 信条セクション（#heart）実装仕様

紅の頁・写真なし。canvas: https://claude.ai/code/artifact/85fc7ce5-3803-4afd-b1a6-40ffb2286f08

この文書はセッションの生存に依存せずに実装を再現するためのものです。数値には出所を付けています。

---

## 0. 確定事項

| 項目 | 決定 | 出所 |
|---|---|---|
| 地 | 紅の頁（色相は変えない） | 実測125境界のうち色相が動いたのは3回=2.4% |
| 写真 | **ゼロ** | クライアント裁定。文章は「人」の話なのに写真は「部屋」の話でレジスタが不一致 |
| 構成 | 群言堂型・1章=1主張・左右交互・柱は縦組み。**尺は借りない** | 群次堂は独立1ページで6章×100vh、こちらは11節中の3番目 |
| コピー | 一字一句変更不可（改行位置のみ字数から逆算して調整） | クライアント承認済み |
| 転換 | 地色・レイアウト・密度の3要素同時 | 実測で1境界3要素同時が最頻42%、1要素だけは14% |

### コピー（変更不可）

- 導入：やまと不動産の信条は「この金額で、ここまでできるんだ」と驚いてもらえる家づくりです。
- 一：「この予算では無理かも」と思っていた人にこそ、もっといい家が、その予算で建つことを知っていただきたいです。
- 二：毎日が楽しい暮らしを、つくりたい。そう考えて、一軒ずつ設計しています。
- 三：5年10年経っても「やまと不動産にお願いして良かった」と言ってもらえる家を、私たちはつくっています。

計180字。**「最初から、いいものを標準で入れています」等の製品の再説明は不採用**（FVの標準装備7景と0円台帳が実演済みで言い直しになる）。

---

## 1. 余白（比率で設計）

**章の内訳 上 : 本文 : 下 = 29 : 43 : 28**（群言堂 `/philosophy/` 実測 260:384:256）

```
章高 = 本文高 ÷ 0.43
```

| ブロック | 本文高 | 章高 | 上 | 下 |
|---|---|---|---|---|
| 導入（2行） | 107px | 247px | 72px | 68px |
| 章一・二・三（各2行） | 80px | 186px | 54px | 52px |

**隣接章の余白は相殺する（大きい方を採る）。** 群言堂は1章=100vhなので余白が積み重ならないが、連続する節では前章の下と次章の上が二重になる。相殺しないと PC 805px・SP 1261px まで伸び、章間に210pxの空洞ができた。相殺後 **PC 647px / SP 1046px**。

- 節の天 = 導入の上余白 **72px** ／ 節の地 = 章三の下余白 **52px**（天>地。HIGASHIYA「空白は下に余らせず上に置く」と同方向）
- 版面 **76..1364**（群言堂 5.27vw）
- 段幅 **本文760px / 導入900px の2段構え**（導入だけ広い。引用を1行に収めるため。下の §3 を見ること）
- 左右交互。**章の箱は `width:max-content` で実際の行長まで縮める**
  （760px固定のままだと右寄せの章二で柱が本文から約400px離れて漂う。実測で本文は255pxしか使わない）
- **左右交互は 1200px 以上でだけ効かせる。** それ未満では列が版面いっぱいになり、
  ずれが柱の幅程度しか出ない（実測: 768pxでは章二が章一より**左**に出た）

---

## 2. 背景アセット — 生成済み `page_beni_dark_{1800,2400}.webp`

**生成モデルではなく決定論的な再トーンで作った。** 上限 L≤0.0508 を確実に満たす必要があり、
生成物は必ず後処理が要る（かつ Higgsfield は当時 認証切れ）。
生成器は `tools/make_page_beni_dark.py`（`page_beni.webp` を入力に、下の目標値へ収束するまで自動で補正）。

**実測（出荷物）**

| 幅 | Lmax | 最明最暗比 | 対 #F3EEE3 | サイズ |
|---|---|---|---|---|
| 1800 | 0.0497 | 1.84 | 9.10 | 126KB |
| 2400 | 0.0501 | 1.78 | 9.07 | 207KB |

描画後（`background-size:cover`）の本文矩形の最悪画素で **PC 9.35〜9.56 / SP 9.42〜9.80**（床7.83）。

### 作るときに引っかかった点（再現時の注意）

1. **リサイズが天井を突き上げる。** 2400で 0.0508 に収めた画像を1800へ縮小すると 0.0549 になる（lossless でも同値＝補間由来）。
   → **先にリサイズしてから色を乗せる**。天井は出力解像度で保証する
2. **WebP圧縮がさらに突き上げる。** q88既定で 0.0623（比2.59）。`use-sharp-yuv` と q93 でも残るため、
   **符号化後に実測して天井を割るまで元の明度を下げる**ループで収束させている
3. **金の粒は入れない。** 床を満たす最明の金 `#4E3D16` を粒として撒いたら、紅の上では金でなく**黴のシミ**に見えた（目視で却下）
4. **フラットフィールド補正だけだと繊維も消える。** 素材の低周波傾斜を割り算で除去すると、残るのはほぼ平坦な塗り。
   絹目の織り（周期2.6px相当）と楮の繊維（長さ6〜34px相当の細い筋）を**作り直して足している**
5. **四隅の牡丹が残る。** 装飾は中周波なので低周波除去では消えない。**元画像の中央66%だけを使う**

### 目標値（再生成する場合）

### 明度
- 全面の相対輝度 **≤ 0.0508**（本文 `#F3EEE3` に対して **9.00 以上**）。**符号化後の実測値で判定する**
- 絶対の床は 0.0659（=7.83）。生成のばらつきを見て 9.00 を狙う
- 平面色の目安：`#7B1218`（L=0.0471 / 9.35）〜 `#8B141B`（L=0.0607 / 8.20）
- **`#BD1B21` は L=0.1171 で 5.43。そのままでは本文を載せられない**

### 明暗差
- 最明/最暗を **2.0倍以内**に。現行は 3.42倍
- **中心を明るくしないこと**。`page_beni` は紙自体の明るい中心部が `background-size:cover` で拡大されて床を割った（違反画素を粗視化すると左上象限がまるごと出る）

### 金
- **文字の下に金は置けない。** 床を満たす最明の金は `#4E3D16`（L=0.0502 / 9.06）で、もはや金に見えない
- `#C9A24B` = 2.07、`#F1D57D` = 1.25、`#6A521F` = 6.39（いずれも不可）
- → 箔・金葉は**文字が載らない領域**（左右の余白・天地）に限る。面積は画面の **8%以下**

### 繊維
- 絹目和紙の織り目は残す。`brightness` で沈めると繊維が潰れるので、**最初からこの明度で生成する**
- 繊維のハイライトが白く飛ばないこと

### 避けるもの
中央のグラデーションハイライト／白飛び／写真的な陰影／彩度の高いオレンジ寄りの金／四隅の大きな装飾図柄

### 減光の方式
**CSSでの減光そのものを廃止した。** `filter:brightness()` も暗幕も使わない（明度はアセット側で確定済み）。
なお方向性スクリムは元々使えない — 左に寄せた減光は片寄せを明度でも支えられて筋が良いが、**章が左右交互なので右段に来る章が 7.83 ちょうどまで落ちる**（実測）。

---

## 3. 書体

| 用途 | サイズ | line-height | letter-spacing | weight |
|---|---|---|---|---|
| 本文 | 20px | 2.00 | 0.085em | 400 |
| 導入 | 28px | 1.9 | 0.08em | 400 |
| 柱（縦組み） | 16px | — | 0.42em | 400 |

出所：菊乃井（文章のみのページ）本文 20px・lh 40px・ls 1.7px（=0.085em）／たねや 全要素 w400／柱16pxは検査の下限。

- 書体は `Shippori Mincho B1`（本文・導入・柱）、ラベル類は `Zen Kaku Gothic New`
- 文字色は**実色 `#F3EEE3`**。透明度を使わない（α指定は実測でコントラストが床を割る）
- **段落は全て2行**（菊乃井の2〜5行の内側）。1行は句・読点で割って **14〜29字**、孤立行を作らない
- **1行の字数上限はフォントサイズごとに変わる。** 760px段なら 20px本文=29字（≒629px）、28px導入=25字。
  「29字」という数字だけを単独で持ち回らないこと

### 導入だけ段幅900px（引用を割らないため）

導入を760px段で組むと1行25字が上限になり、**クライアントの言葉「この金額で、ここまでできるんだ」が行をまたいで割れる**。
導入だけ `--lcol:900px` にすると28字＝847pxが1行に収まり、引用を保ったまま2行になる。
本文760・導入900の2段構え。**節の高さは647pxのまま変わらない**（行数が同じなので）。

```
✅ やまと不動産の信条は「この金額で、ここまでできるんだ」と / 驚いてもらえる家づくりです。
❌ やまと不動産の信条は「この金額で、 / ここまでできるんだ」と驚いてもらえる家づくりです。
```

**820px未満ではこれが入らない**（実測: 800px=2行 ✅ / 780px=3行 ❌、chromium・webkit 同値）。
そこだけ引用の内側で割る。導入の改行は次の3系統になる。

| クラス | 割る位置 | 有効な幅 |
|---|---|---|
| `.b-sp` | 信条は / 驚いてもらえる の後ろ | ≤760 |
| `.b-md` | 「この金額で、 の後ろ（引用の内側） | ≤819 |
| `.b-lg` | ここまでできるんだ」と の後ろ（引用の外側） | ≤760 と ≥820 |

`.b-md` と `.b-lg` は **SPでも必要**（SPは5行に割るので4箇所すべてに改行が要る）。
中間帯 761〜819 でだけ `.b-lg` を止めて `.b-md` に渡す。
この2つを「PC専用」として扱うと**760pxで導入が3行に落ちる**（一度やった）。

### 改行位置（PC）

```
導入 やまと不動産の信条は「この金額で、ここまでできるんだ」と / 驚いてもらえる家づくりです。   ← 820px以上
     やまと不動産の信条は「この金額で、 / ここまでできるんだ」と驚いてもらえる家づくりです。   ← 761〜819px
一   「この予算では無理かも」と思っていた人にこそ、 / もっといい家が、その予算で建つことを知っていただきたいです。
二   毎日が楽しい暮らしを、つくりたい。 / そう考えて、一軒ずつ設計しています。
三   5年10年経っても「やまと不動産にお願いして良かった」と / 言ってもらえる家を、私たちはつくっています。
```

### SP（390px）

段幅 **306px**（左64・右20）。本文 **16px = 1行17字上限**、導入 **20px = 1行14字上限**。この字数から逆算して改行を打ち直す。

```
導入 やまと不動産の信条は / 「この金額で、 / ここまでできるんだ」と / 驚いてもらえる / 家づくりです。
一   「この予算では無理かも」と / 思っていた人にこそ、 / もっといい家が、その予算で / 建つことを知っていただきたいです。
二   毎日が楽しい暮らしを、 / つくりたい。そう考えて、 / 一軒ずつ設計しています。
三   5年10年経っても / 「やまと不動産にお願いして / 良かった」と言ってもらえる家を、 / 私たちはつくっています。
```

柱は全て左（SPでは左右交互にしない）。

### 360px以下

390px設計のままだと「建つことを知っていただきたいです。」（17字＝295px）が段幅240pxに入らず、
**句の途中で自動折返しする**。`.b-xs` で一・三に専用の改行を足し、`--lines` を5にして比率を保つ。

```
一 …その予算で / 建つことを / 知っていただきたいです。
三 …「やまと不動産にお願いして / 良かった」と / 言ってもらえる家を、 / 私たちはつくっています。
```

---

## 4. モーション

- 入場 `1.6s cubic-bezier(.16,1,.3,1)`、`opacity 0→1` ＋ `translateY(24px)→0`
- 章ごとに 0 / 0.18 / 0.36 / 0.54s のディレイ
- `prefers-reduced-motion: reduce` で**完全無効**（`animation:none; opacity:1; transform:none`）
- 出所：実測で たねや 2s、イムラ 1s。0.3s前後の短いフェードは高級側に実例なし

---

## 5. 実測値（設計時・代用地）

**⚠️ この節は設計段階の値。出荷物の実測は §7 を見ること**（地が専用アセットに変わり、コントラストは 9.45〜9.84 に上がっている）。

### PC 1440 × 647

| 章 | 章高 | 本文 | 字数 | 行数 | 字/900px | 本文コントラスト |
|---|---|---|---|---|---|---|
| 導入 | 247px | 107px | 42字 | 2行 | 153 | 8.49 |
| 一 | 186px | 80px | 53字 | 2行 | 256 | 8.91 |
| 二 | 186px | 80px | 35字 | 2行 | 169 | 10.72 |
| 三 | 186px | 80px | 50字 | 2行 | 242 | 10.68 |

節 647px / 180字 = **250字per900px**（写真あり995px版は163）

### SP 390 × 1046

本文コントラスト 9.26〜11.07（全て床超え）／ **155字per900px**

### 閾値に対する評価

| 閾値 | 結果 |
|---|---|
| 本文コントラスト 7.83 | **全ブロック超過**（最小 8.49） |
| 写真面積 対画面比 0.1〜0.5 | 対象外（写真ゼロ） |
| 300字/900px | **未達 250**。承認コピーが全180字なので、300を満たすには節が540px以下でなければならず「余白が主役」と両立しない。写真という逃げ道も無くなったため、余白を優先して250で止めた。995→647で 163→250 に改善 |

**コントラストは各ブロック矩形内の「最も明るい画素」で測った最悪ケース値。** 平均で測ると通ってしまう。

---

## 6. 測り方（再現手順）

```bash
# 地だけを描画（文字・写真なし）してから、文字が載る矩形の最明画素を取る
magick ground.png -crop {w}x{h}+{x}+{y} +repage -depth 8 RGB:- \
  | # 画素ごとに 0.2126R+0.7152G+0.0722B（sRGB逆ガンマ後）を計算し最大値を取る
# コントラスト = (L_text + 0.05) / (L_bg_max + 0.05)   ※L_text(#F3EEE3) = 0.8575
```

注意：`border-radius` の角の外は白（親の地）が写るので、測定範囲から外すこと。多層セクションを全体で測らない。撮影範囲が節の境界と一致しているか、測る前に確認する。


---

## 7. 実装後の実測（2026-08-19・`heart_mock.html`）

`mobile-verify` webkit/chromium × 10幅 = **20/20 FAIL 0 WARN 0**。

| 幅 | 節の高さ | 字/900px | 導入 | 一 | 二 | 三 | 上比 / 下比 |
|---|---|---|---|---|---|---|---|
| 1440 | 647px | 250 | 2行 | 2行 | 2行 | 2行 | 0.675 / 0.650 |
| 820 | 558px | 290 | 2行 | 2行 | 2行 | 2行 | 0.676 / 0.648 |
| 1280 | 624px | 260 | 2行 | 2行 | 2行 | 2行 | 0.667 / 0.641 |
| 1199 | 613px | 264 | 2行 | 2行 | 2行 | 2行 | 0.671 / 0.658 |
| 1024 | 588px | 276 | 2行 | 2行 | 2行 | 2行 | 0.676 / 0.649 |
| 768 | 551px | 294 | 2行 | 2行 | 2行 | 2行 | 0.671 / 0.643 |
| 430 | 1059px | 153 | 5行 | 4行 | 3行 | 4行 | 0.674 / 0.651 |
| 390 | 1047px | 155 | 5行 | 4行 | 3行 | 4行 | 0.674 / 0.653 |
| 360 | 1174px | 138 | 5行 | 5行 | 3行 | 5行 | 0.675 / 0.650 |
| 320 | 1174px | 138 | 5行 | 5行 | 3行 | 5行 | 0.675 / 0.650 |

目標比 0.674 / 0.651 に対し全幅で 0.667〜0.681 / 0.641〜0.658。**1440で647px は設計値と一致**。

余白は `margin` の相殺に任せている（`overflow:hidden` で節をBFC化し、章の余白が節の外へ抜けないようにしたうえで、
節自身の上下 padding は 0）。行数は `--lines` として各ブロックに持たせ、
`margin-top: calc(var(--lines) * 2em * 29/43)` の形で比率を直接書いている。
