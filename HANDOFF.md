# 引き継ぎドキュメント（Cursor → 次エージェント）

作成日: 2026-08-10 ／ 作成者: Cursor エージェント
対象: 本プロジェクトを引き継ぐすべての AI エージェント・開発者

---

## 1. まず読むもの（読む順）

1. **`AGENTS.md`**（※リポジトリの1階層上、`最終やまと不動産画像ファイル/` 直下にある。§2参照）— 全エージェント共通の憲章。絶対ルールはここが正。
2. `大和不動産_専務打ち合わせ内容まとめ.md`（同じく1階層上）— 戦略・メッセージの一次ソース。
3. `やまと不動産_サイト構造ドキュメント.md`（同じく1階層上）— 現サイト分析。「乗り越える対象」としてのみ使う。
4. 本ファイル — 実装の現状と技術的な引き継ぎ事項。

## 2. 【最重要】リポジトリと作業フォルダの関係

- **この git リポジトリの実体はローカルの `最終やまと不動産画像ファイル/デザイン図面_20260703/`**。リポジトリ直下に `index.html` 等が並ぶ。
- **戦略ドキュメント（AGENTS.md / 専務打ち合わせまとめ / サイト構造ドキュメント / 各種調査md）はリポジトリの外**（1階層上のプロジェクトルート）にあり、**git 管理されていない**。
  - つまり **GitHub を clone しただけでは一次情報が手に入らない**。別マシンで作業する場合は、プロジェクトルートごと引き継ぐこと（オーナーの Mac 上: `/Users/takahirokamino/Downloads/最終やまと不動産画像ファイル/`）。
- remote: `https://github.com/tkaminobiz-art/YAMATO-FINAL.git` ／ ブランチ: `main` のみ。
- リモートは現在 **public**。クライアント案件のため公開可否はオーナー確認事項（未解決）。

## 3. 絶対ルール（要約。正は AGENTS.md）

- 完全な新規プロジェクト。**`YAMATOFUDOUSANV2`（V2）は競合であり参照禁止**。構成・情報設計・成果物を真似ない・引用しない。
- FV（ファーストビュー）に物件を出さない。物件・土地は「出口」に配置。
- 文字だらけNG。写真・動画メイン。**スマホ最優先**。
- ゴール: 自社サイト単体で問い合わせ・来店獲得（脱SUUMO依存）。ターゲットは大阪・京都からの移住検討層。
- 芯のメッセージ: 「見せたものが、そのまま届く」（追加費用ゼロ・コミコミ）／「この金額で建つ驚き」。

## 4. クライアント裁定・確定事項（変更禁止。コミット履歴に根拠あり）

| 項目 | 確定内容 | 根拠コミット |
|---|---|---|
| 「増えません」の基準点 | 見積もり時ではなく**契約・着工後**を基準に表現する（2026-07-08 クライアント裁定） | `f73cfab` |
| 価格表記 | **2,480万円〜** | `f568dcb` |
| 役員役職 | 古谷=会長 ／ 小林=社長 | `6e3efa7` |
| 引用出典 | 会社案内の引用は実名表記 | `cac480d` |
| FV | 「あかりの物語・改」v4 を採用（`fv_cinema.html` 図面から `index.html` へ統合済み） | `149d168` |

## 5. ページ構成

### 本番ページ（相互リンク済みの公開想定セット）

| ファイル | 役割 |
|---|---|
| `index.html` | トップ（FV「あかりの物語・改」= GSAP pin/scrub のスクロールシネマ。iju節=難波→生駒20分の夜の帰路演出を含む） |
| `kodawari.html` | 家づくりのこだわり（#price / #standard / #design / #after のアンカーで各所から参照） |
| `works.html` | 施工事例 |
| `madori.html` | 間取り・商品ラインナップ |
| `lots.html` | 自社分譲物件一覧（=「出口」） |
| `model.html` | モデルハウス「やまとランド」 |
| `voice.html` | お客様の声50通 |
| `company.html` | 会社案内 |
| `contact.html` | 来場予約・問い合わせ（`?type=reserve` / `?type=docs` / `#line` のパラメータ・アンカーを各CTAが利用） |

### 図面・実験ページ（本番導線には未接続。検討過程の成果物）

`fv_cinema.html`（FV原図面）, `index_iju_dark.html`（トップ暗色バリアント）, `naze.html`, `lineup.html`, `blueprint.html`, `authority-*.html`, `decoration-*.html`, `section-*.html`, `fv.html`, `header.html` ほか。削除せず残置（検討の記録）。

### git 管理外（ローカルのみ・コミット不要）

- `works.backup_preMuku_061554.html`, `assets/works_ungraded_backup/` — 作業前のローカルバックアップ（未追跡のまま放置でよい）
- `index.backup_*.html`, `company_shoin_rejected.html`, `assets/fv_candidates/`, `参考FV_20260704/`（他社キャプチャ・再配布不可）— `.gitignore` 対象

## 6. 技術スタック

- **ビルド無しの静的HTML**。各ページにCSS/JSをインライン＋CDN読み込み。フレームワーク・npm不使用。
- アニメーション: **GSAP 3.13.0 + ScrollTrigger**（CDN）、スムーススクロール: **Lenis 1.3.25**（CDN）。
- フォント:
  - `index.html` / `works.html` / `madori.html` = **Google Fonts**（Shippori Mincho / Zen Kaku Gothic New / Klee One / Inter / Noto Sans JP / Cormorant Garamond）
  - それ以外の多くのページ = **Adobe Typekit `aym8vre`**（→ §8 の注意点参照）
- 画像・動画: `assets/`（約645MB）。FV外観は nano_banana_pro でリライト済みの実写ベース画像。3D素材（.glb）は実験用。
- プレビュー方法: リポジトリ直下で `python3 -m http.server 8000` → `http://localhost:8000/`。スマホ実機確認は同一LANで `http://<MacのIP>:8000/`（過去に `_*_qr.png` のQRを使用）。

## 7. 2026-08-10 時点の git 状態

- `main` = `origin/main` = `6e3efa7`（最終コミット 2026-07-09「役員役職の変更を反映」）。push漏れなし。
- 本日、`index.html` / `index_iju_dark.html` / `kodawari.html` が**未コミットのままローカル削除された状態**になっていたのを発見し、`git checkout -- <files>` で HEAD から復元済み（内容の損失なし）。原因不明のため、同事象が再発したら安易に削除をコミットしないこと。
- 未追跡ファイルは §5 のローカルバックアップ2点のみ。

## 8. 既知の課題・未解決事項（次エージェントへのTODO候補）

1. **Typekit キットの混在**: コミット `8b3f7e9` で kit を `ftb5hqp` に差し替えた記録があるが、現在のコードに `ftb5hqp` への参照は無く、`company.html` / `contact.html` / `kodawari.html` / `lots.html` / `voice.html` / `model.html` など本番下層ページは旧 kit `aym8vre` のまま。旧kitはドメインロックで**ローカルでは発火しない**とされていたため、下層ページのフォント表示を実機で確認し、必要なら統一すること。
2. **キャッチコピー未確定**: サイト構成が固まってから逆算で確定する方針（AGENTS.md §6）。現状は各ページの見出しが実質のコピー。
3. **リポジトリの公開可否**: 現在 public。クライアント案件のためオーナーに要確認。
4. **図面ページの整理**: 検討過程の図面・実験ページが多数リポジトリに残っている。公開前に本番セットと分離するか判断が必要。
5. **戦略ドキュメントのリポジトリ外問題**: §2 の通り。別環境へ移行するなら一次情報の受け渡し方法を決めること（public repo にはクライアント内部情報を置かない判断も含めて）。

## 9. Git 運用ルール（厳守）

```
cd デザイン図面_20260703        # ← 必ずリポジトリ直下で git を叩く（1階層上は git 管理外）
git pull --rebase              # 他エージェントの push を先に取り込む
git add <自分が触ったファイルだけ>  # git add -A 禁止（複数エージェント同時編集のため）
git commit -m "..."
git push origin main
```

- コミットメッセージは日本語・「何をなぜ」が分かる形式（`git log` の既存スタイルに合わせる）。
- 大きな方針変更・破壊的操作の前はオーナーに確認。
